!(function(e, t) {
  var transforPhoneArr = [{
      name : "SM-N9500",
      origin : 412,
      target : 375
  }];
  function n() {
    t.body
      ? (t.body.style.fontSize = 12 * o + "px")
      : t.addEventListener("DOMContentLoaded", n);
  }
  function d() {
    var w = i.clientWidth;
    for(var k = 0;k<transforPhoneArr.length;k++){
        if(navigator.userAgent.includes(transforPhoneArr[k].name) && w == transforPhoneArr[k].origin){
           w = transforPhoneArr[k].target;
           break;
        }
    }
    var e = w / 10;
    i.style.fontSize = e + "px";
  }
  var i = t.documentElement,
    o = e.devicePixelRatio || 1;
  if (
    (n(),
    d(),
    e.addEventListener("resize", d),
    e.addEventListener("pageshow", function(e) {
      e.persisted && d();
    }),
    o >= 2)
  ) {
    var a = t.createElement("body"),
      s = t.createElement("div");
    (s.style.border = ".5px solid transparent"),
      a.appendChild(s),
      i.appendChild(a),
      1 === s.offsetHeight && i.classList.add("hairlines"),
      i.removeChild(a);
  }
})(window, document);
